###  Что такое Python?
#### Короткий ответ
Python - это высокоуровневый интерпретируемый язык программирования общего назначения с динамической, строгой типизацией, который ценится за простоту синтаксиса и широкую область применения.

#### Развернутый ответ
Python — это высокоуровневый язык программирования, отличающийся эффективностью, простотой и универсальностью использования. Он широко применяется в разработке веб-приложений и прикладного программного обеспечения, а также в машинном обучении и обработке больших данных. За счет простого и интуитивно понятного синтаксиса является одним из распространенных языков для обучения программированию.

### Что такое динамическая и строгая типизация?

#### Короткий ответ
Динамическая типизация - проверка типов происходит во время выполнения.
Строгая типизация - не допускает неявные преобразования между разными типами.

#### Развернутый ответ
- **Динамическая типизация** (Python, JavaScript):
  - Тип переменной определяется в момент присваивания значения
  - Одна переменная может менять тип в ходе выполнения программы
  - Пример:
    ```python
    x = 10      # int
    x = "text"  # str - тип изменился
    ```

- **Строгая типизация** (Python, Java, C#):
  - Явное преобразование типов требуется для операций между разными типами
  - Пример:
    ```python
    print(10 + "5")  # TypeError в Python
    print(10 + int("5"))  # Явное преобразование
    ```

#### Доп. информация
- Python - язык с динамической строгой типизацией:
  - Динамическая: типы проверяются при выполнении
  - Строгая: требует явного преобразования типов
- **Слабая типизация** (C, JavaScript):
  - Позволяет неявные преобразования типов
  - Пример:
    ```javascript
    console.log("5" * 2);  // 10, неявное преобразование
    ```

- **Статическая типизация** (Java, C#):
  - Проверка типов происходит на этапе компиляции
  - Тип переменной фиксируется при объявлении
  - Пример:
    ```java
    int x = 10;  // x всегда будет int
    ```

- **Смешанная типизация** (Scala, Kotlin):
  - Комбинирует элементы статической и динамической типизации
  - Позволяет использовать как статическую, так и динамическую типизацию в зависимости от контекста

### Python — интерпретируемый язык или компилируемый?

#### Короткий ответ  
Python — **интерпретируемый**, но код сначала компилируется в байт-код (`.pyc`), который затем выполняется виртуальной машиной (CPython).  

#### Развернутый ответ  
Python считается **интерпретируемым** языком, так как исходный код выполняется построчно интерпретатором, а не преобразуется заранее в машинный код. Однако перед выполнением Python компилирует исходный код в **байт-код** (сохраняемый в `.pyc` файлах), который затем исполняется виртуальной машиной Python (например, CPython).  

**Пример:**  
При запуске Python-скрипта автоматически создаётся `.pyc`-файл:  
```python
# hello.py  
print("Hello, world!")  
```  
После первого выполнения в папке `__pycache__` появится:  
```
__pycache__/hello.cpython-*.pyc  
```  

Этот механизм ускоряет последующие запуски программы.

### Python — декларативный или императивный?

#### Короткий ответ  
Python — **преимущественно императивный** язык (код описывает "как делать"), но поддерживает и **декларативные элементы** (например, генераторы списков, SQLAlchemy).

#### Развернутый ответ  
Python в первую очередь является **императивным** языком программирования. Это означает, что:  
- Программист явно описывает последовательность шагов для выполнения задачи  
- Управление потоком выполнения осуществляется через условия, циклы и функции  
- Состояние программы изменяется через присваивания  

Однако Python также включает **декларативные** возможности, где программист описывает "что нужно получить", а не "как это сделать":
- Генераторы списков/словарей/множеств  
- Декораторы  
- ORM-библиотеки типа SQLAlchemy  
- Функциональные элементы (map, filter, lambda)  

**Примеры стилей:**  

```python
# Императивный стиль (явное описание шагов)
squares = []
for i in range(5):
    squares.append(i * i)

# Декларативный стиль (описание результата)
squares = [i * i for i in range(5)]  # list comprehension
```

Таким образом, Python — это гибкий язык, позволяющий сочетать оба подхода, но с преобладанием императивной парадигмы.

### Что такое PEP-8?

#### Короткий ответ  
**PEP-8** — официальное руководство по стилю кода на Python, содержащее стандарты форматирования для улучшения читаемости и единообразия кода.  
PEP в Python расшифровывается как ==Python Enhancement Proposal==, что переводится как "Предложение по улучшению Python". Это документы, описывающие новые функции, изменения в языке, процессы и рекомендации для сообщества Python.

#### Развернутый ответ  
PEP-8 (*Python Enhancement Proposal 8*) — это набор **рекомендаций** по оформлению Python-кода, включающий:  

✅ **Именование**  
- `snake_case` для функций и переменных  
- `PascalCase` для классов  
- `UPPER_CASE` для констант  

✅ **Форматирование**  
- Отступы: **4 пробела** (не табы)  
- Максимальная длина строки: **79 символов**  
- Пробелы вокруг операторов (`=`, `+`, `-` и т.д.)  

✅ **Организация кода**  
- Импорты: группировка (стандартные → сторонние → локальные)  
- Пустые строки для разделения логических блоков  

**Пример кода по PEP-8:**  
```python
def calculate_sum(a: int, b: int) -> int:
    """Возвращает сумму двух чисел."""
    return a + b
```

⚠ **Важно:**  
Хотя PEP-8 — это рекомендации (не строгие правила), их соблюдение упрощает совместную работу над проектами. Для автоматической проверки стиля используют **linters** (например, `flake8` или `pylint`).  

🔗 [Официальный текст PEP-8](https://peps.python.org/pep-0008/)

### Что такое интроспекция (introspection)?

#### Короткий ответ  
**Интроспекция** — это возможность программы анализировать объекты **во время выполнения** (например, через `type()`, `dir()`, `hasattr()` или `__dict__`).  

#### Развернутый ответ  
Интроспекция (*introspection*) — это механизм, позволяющий **изучать структуру и свойства объектов** прямо во время работы программы. Python предоставляет множество встроенных инструментов для этого:  

✅ **Основные функции:**  
- `type(obj)` → возвращает тип объекта (`int`, `str`, `class` и т.д.)  
- `dir(obj)` → список всех атрибутов и методов объекта  
- `hasattr(obj, 'attr')` → проверяет наличие атрибута  
- `getattr(obj, 'attr')` → получает значение атрибута  
- `isinstance(obj, class)` → проверяет принадлежность к классу  

✅ **Примеры:**  
```python
x = 42

print(type(x))          # <class 'int'>
print(dir(x))           # ['__abs__', '__add__', ...]
print(hasattr(x, 'bit_length'))  # True (у int есть метод bit_length)
```

✅ **Продвинутая интроспекция:**  
- `obj.__dict__` → атрибуты объекта в виде словаря  
- `inspect` (модуль) → детальный анализ функций, классов и т.д.  

**Зачем это нужно?**  
- Динамическое изменение поведения программы  
- Отладка и логирование  
- Создание гибких API (например, ORM, сериализация)  

🔹 *Пример использования в реальных проектах:*  
```python
def dump_object(obj):
    """Выводит все атрибуты объекта."""
    for attr in dir(obj):
        if not attr.startswith('__'):
            print(f"{attr}: {getattr(obj, attr)}")
```  

Python — один из лучших языков для интроспекции благодаря своей динамической природе.
